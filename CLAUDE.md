# Director — rules

This repository is the Director's record. It publishes nothing and ships no code.

**The rules are `CLAUDE.md` on `samusmylove47-maker/eql-source`, branch `main`.**
They govern the site, they govern this post, and they are binding here.

**This file is a pointer and will never be a copy.** A copy goes stale, and a
figure typed beside the data it claims to come from is the fault this project
keeps finding in other people's work. Read the constitution at its source:

```bash
git clone https://github.com/samusmylove47-maker/eql-source /tmp/eqls
git -C /tmp/eqls show origin/main:CLAUDE.md
```

Then `DIRECTOR-ONBOARDING.md` for the post, and `HANDOFF.md` for the live record.
